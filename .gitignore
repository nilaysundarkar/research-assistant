.env
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/
.ipynb_checkpoints/
*.egg-info/
.DS_Store

# Trace logs from ad-hoc CLI sessions are noisy; ignore them.
traces/*.jsonl

# NOTE: eval artifacts (eval/results/) and viz outputs (viz/output/)
# are deliberately TRACKED so a grader can verify the project without
# spending money on API calls — they are intentionally NOT listed here.